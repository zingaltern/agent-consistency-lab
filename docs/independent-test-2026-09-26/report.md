# 独立黑盒测试报告（模板：**未完成，待真人执行**）

> 🚧 **本文件是骨架，不是报告。** 本仓库的红线（`AGENTS.md` 7、`docs/testing.md` §4-7）规定：
> 独立测试必须由**真人**执行并把结论写在这里；**agent 不得代跑、不得替填、不得声称已完成**。
> 执行时请把本目录复制成 `docs/independent-test-<实际执行日期>/`，然后在副本里填写。
>
> 填完请把本节横幅删掉，并在顶部写明：执行者（人或 agent 辅助哪几步）、日期、环境
> （Python 版本、机器）、以及"哪些结论是我自己跑出来的、哪些只是转述"。

---

## 0. 方法与证据链

* 读了哪些"规格"（`README.md` / `docs/semantics.md` / `docs/HANDOFF.md` / 源码与 `--help`）：
* 什么时候才读"答案"（`docs/w2..w7` / `reports/` / `tests/` / `docs/posts/` / `docs/resume.md`）：
* 我的环境（Python、机器、是否本机空闲）：
* 我的产物目录（应为 `/tmp` 下）：`/tmp/independent-test-<日期>/`
* 一键入口的四套基线结果与退出码（照抄 `commands.sh` 的汇总，别手改数字）：

| 命令 | 退出码 | 要点 |
|---|---|---|
| `pytest -o addopts= -p no:cacheprovider -q` | | 用例数见 `count_tests.py` 的产物 |
| `ruff check .` | | |
| `scripts/check_facts.py --run verify` | | 通过条数 |
| `experiments.crash_matrix --repeats 5` | | 16 格是否逐格 `as-predicted` |

## 1. 各方向结论表（对应 `docs/tester-prompt.md` §3 的 A–F）

| 方向 | 我的结论 | 证据（命令 + 输出要点） | 与作者声称是否一致 |
|---|---|---|---|
| A 可复现性 | | | |
| B 承诺逐条证伪 | | | |
| C 崩溃语义（外部账本） | | | |
| D 审批语义 | | | |
| E 评测口径与门禁 | | | |
| F 边界诚实性 | | | |

## 2. 缺陷列表（每条：标题 / 严重度 / 最小复现 / 期望 vs 实际 / 建议）

* （P0）…
* （P1）…
* （P2）…

严重度定义：**P0** 某条结论不成立（机制层面被证伪）；**P1** 声称与实现不符但结论仍成立；
**P2** 文档/口径与事实不符。

## 3. 无法判定的项

* 写清"为什么无法判定"（缺什么工具、缺什么权限、还是需要更长的实验窗口），不要猜。

## 4. 未覆盖的风险（我认为我没测到的部分）

* 例如：只跑了本机、只测了单进程、没有真实模型、没有掉电环境等。

## 5. 与作者声称的差异

* 允许结论是"无差异"。逐条列出"作者说 X，我实测 Y"，并给出复现命令。

## 6. 证据索引

* 逐条结论 → 命令 → 产物文件（`/tmp/...`）→ 关键输出片段。
