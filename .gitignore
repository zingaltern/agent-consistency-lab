__pycache__/
*.py[cod]
.venv/
.venv*/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.db
*.db-wal
*.db-shm
.artifacts/

# 逐次运行明细：体积大且可由一条命令再生（报告表格与汇总入库）
reports/*_runs.json
.DS_Store

# 本机会话与对外材料：不进版本库（开源仓库不发布这两类内容）
# .zcode/ 是本地 agent 会话的临时计划文件；packaging/ 是对外口径材料草稿，
# 与仓库自身"逐条可核验"的诚实口径冲突，只保留在本地磁盘。
.zcode/
packaging/
